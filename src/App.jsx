import { useState, useMemo, useCallback } from 'react';
import ListItems from './components/ListItems';
import './App.css'

function App() {
  
  const [search, setSearch] = useState("");
  const [count, setCount] = useState(0);

  const item = ["apple", "banana", "grapes", "guava", "cherry", "mango"];

  return (
    <>
      
    </>
  )
}

export default App
